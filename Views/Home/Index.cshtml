@{
    ViewData["Title"] = "Home Page";
    Layout = "~/Views/Shared/_LayoutThucHanh.cshtml";
}
@model List<TSach>

@foreach(var item in Model)
{
       @await Html.PartialAsync("PartialSach", item);
}

